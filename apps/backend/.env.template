# منبع کانفیگ Docker: فایل `.env` در ریشهٔ monorepo (کنار `docker-compose.yml`).
# الگو: `../../../.env.template` — از ریشهٔ ریپو: `cp .env.template .env`
#
# برای `medusa develop` از این پوشه، می‌توانید env ریشه را لینک کنید:
#   ln -sf ../../../.env .env
