/**
 * Mirrors src/lib/feature-access/modules.ts (kept separate: admin bundle
 * can't reach into server-only backend code). Keep both lists in sync.
 */
export const FEATURE_MODULE_NAMES = ["remitation"] as const
